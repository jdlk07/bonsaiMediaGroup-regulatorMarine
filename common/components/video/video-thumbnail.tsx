import { VideoModel } from '../../../lib/umbraco/types/videoModel.type';
import styles from './video.module.scss';

export default function VideoThumbnail(data: VideoModel) {
    return (
        <div className={styles.videoThumbnail}>

        </div>
    )
}